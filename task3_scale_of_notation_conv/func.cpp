#include <iostream>
#include "func.h"



int convert(int number)
{
	int max = 256;
	int summ = 0;
	char bin = ' ';

	while (max != 1)
	{
		if (number < max)
			max = max / 2;
		else
			if (summ  
			summ = summ + max;
			bin = bin + '1';
	}
	


}